{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="sm:mx-auto sm:w-full sm:max-w-md">
			<h2 class="my-6 text-center text-2xl font-extrabold text-gray-700">
                <span class="text-amber-500 uppercase">{{ user.firstName }}'s</span> Profile
			</h2>
		</div>
    <div class=" bg-white rounded-lg overflow-hidden shadow-lg flex flex-col justify-center py-12 sm:px-6 lg:px-12">

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('user/_delete_form.html.twig') }}

    </div
{% endblock %}
