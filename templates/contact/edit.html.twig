{% extends 'base.html.twig' %}

{% block title %}Edit Contact{% endblock %}

{% block body %}
 <div class="sm:mx-auto sm:w-full sm:max-w-md">
			<h2 class="my-6 text-center text-xl font-extrabold text-gray-700">
                Edit Contact of
               <br> <span class="text-amber-500 uppercase">{{ contact.name }}</span>
                </h2>
		</div>
    <div class=" bg-white rounded-lg overflow-hidden shadow-lg flex flex-col justify-center py-12 sm:px-6 lg:px-12">
    {{ include('contact/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('contact/_delete_form.html.twig') }}
    </div>
{% endblock %}
